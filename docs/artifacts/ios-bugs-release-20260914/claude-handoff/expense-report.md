# Expense release audit — IOS BUGS P9-2

Read-only ops-app project: `ijeekuhbatykdomumfjx`. Catalog and aggregate reads completed September 15, 2026 UTC. No customer-identifying rows, provider reads/writes, migrations, services, builds, simulators, phone operations or database tests were executed. Two subsequently authorized source repairs were committed only in the clean private P5 checkout. Root owns execution and release.

## Release findings

1. **Coordinated runtime cutover remains required.** The lifecycle migration begins capturing/queuing decisions immediately; SQL has no expense-specific delivery switch. Fresh counts at 03:14:03Z show one eligible QuickBooks push connection. Root independently found deployed web commit `b5b0cb59275f0addf923db608b1daeac4afca844` lacks expense processors, the old direct-provider edge remains ACTIVE v5, and the deployed global accounting write flag is true. Applying schema alone can introduce new work to old workers while legacy delivery remains available. Root is preparing a candidate from the actual deployed commit; the shared integration checkout is an audit source, not an approved deployable release. These runtime facts came from root, not a second provider/deployment inspection here.

2. **Post-row-lock identity revalidation was missing in the prepared authority helper.** `private.lock_expense_approver_context()` resolves the JWT's user before `SELECT ... FOR SHARE OF actor,company`. If the actor row is held by a concurrent login-binding edit, the row resumes active and authorized under its old ID after the binding changes. Its original permission check accepts that cached actor. The outer P4/P5 dispatcher already re-resolves identity after the advisory company wait; the uncovered interval is the helper's row-lock wait. The accounting-request caller shares the helper. P7 calls it before both new corrections and exact replay; a new correction also has a current-actor trigger check, while replay returns before that write trigger. Repair checks current user ID and current company ID again after row locks, preserving the existing 42501 error and grants. Source-only preparation: commit `8d88676e3015ab5ed7b1e659f2e8d0cb285dd550`. Runtime proof remains pending.

3. **The event identity sequence inherited unsafe independent grants.** Live postgres default privileges in public grant anon/authenticated/service_role USAGE, SELECT and UPDATE on new sequences. Revoking the event table's grants does not revoke its sequence grants. The original lifecycle migration left clients able to allocate/reset the ordering sequence, despite protecting event rows. The repair resolves the actual identity sequence through `pg_get_serial_sequence` and revokes all grants from PUBLIC and those three roles, preserving owner allocation. All four migrations were scanned: `expense_accounting_events.sequence` is their only new identity/serial sequence. Fixture defaults now reproduce the live grants, and a focused SQL runner calls actual nextval/setval under each role. Source-only preparation: `816eec629967a09f5ca4ac041e3d44abc58c9e5f`. Runtime proof remains pending.

## Fresh compatibility evidence

- All four migration ledger versions absent at 03:14:03Z. Matching accounting/correction tables, new private decision helpers and new RPCs absent; reimbursement_amount absent. No partial-install markers were found in the checked object families.
- At 03:08:13Z: zero claimed accounting rows, zero accepted claims, two nonterminal rows; zero expense queue rows or expense sync log entries; zero historical expense external-ID/synced markers. At 03:14:03Z the nonterminal rows grouped to QuickBooks blocked=1 and needs_review=1. These are snapshots, not locks or authorization to alter existing work.
- One eligible connected/sync-enabled push/bidirectional connection, QuickBooks. No identities, external account values or tokens were read into evidence.
- Zero cross-company expense/batch links, zero cross-submitter links, zero asymmetric paid_at/paid_by markers.
- Actual schema confirms UUID expense/company/submitter/batch identities, text allocation project IDs, nullable numeric allocation amounts, existing queue provider-acceptance fields and text accounting connection company IDs.
- Twenty captured function definitions exactly match the prepared fixture baselines, including the three P7 authority/placement hashes and original payroll MD5 `c3e517bcc781f6866601c5966fa0b58d`. Additional raw/effective permission and company-admin bodies match their captured fixtures. The current atomic-save, response and allocation-touch bodies match the Bible's July 20 atomic-save migration. Current recalculation matches the later July 20 alias correction exactly, rather than its superseded earlier version. `get_or_create_open_batch` also matches the captured fixture.
- Existing queue/event entity/source constraints retain all current non-expense enumerants when the lifecycle migration adds expense. The live `guard_financial_document_distribution` queue trigger only suppresses held estimates and returns expense rows unchanged.
- Existing expense/batch/allocation payroll/artifact/expense revision triggers were captured, including their attached arguments. The main decision fixture includes the live revision lock behavior; the captured generic queue distribution trigger is not installed there, but source inspection shows no expense branch.
- `private` schema USAGE is available to authenticated, as required by P7's public SECURITY INVOKER wrapper to its explicitly granted private implementation. Existing decision RPC ACLs are intentionally preserved. P7 new correction/history functions explicitly revoke all default client/service grants and grant authenticated only. Payroll function authority/grants are preserved by its guarded replacement.

## Executable preflight / installation boundaries

`preflight-readonly.sql` contains only a read-only transaction, catalog reads and aggregate SELECTs. It compares 48 current function fingerprints and reports absence, claims, eligibility, legacy markers, inconsistent batch custody and schema/default-ACL state. It is for the uninstalled baseline; it is not an installer or a lock. `catalog-queries.sql` holds the exact successful source queries, including full column/function/constraint/index/trigger/policy snapshots.

Before applying an authorized release:

- Re-run preflight against the exact target and fail on unexpected baseline hash, type, trigger, constraint, ACL or partial-install state. Original authority/lifecycle migrations do not independently hash-lock the whole live baseline and use IF NOT EXISTS for new objects; they must not silently accept arbitrary partially installed tables.
- Apply the strict dependency order authority → lifecycle → payroll projection → correction. Keep application decisions/workers safe across the entire transition, including the interval between individually committed migrations. An authority-only interval still has the old company-card payout semantics. The payroll file has no explicit BEGIN/COMMIT: execute it transactionally so its revision invalidation and function replacement cannot split.
- Recompute migration manifest SHA256 after the two source repairs; the archived audited hashes remain the originals, not final release hashes.
- After applying, verify complete new function/table/trigger/sequence ACLs and expected replacement hashes. Verify no unexpected PUBLIC/anon/authenticated/service_role sequence privileges; check owner-only allocation. Confirm matching deployed processor/edge versions before permitting accounting writes.
- Re-read claims and mapping/legacy evidence immediately at cutover. Existing blocked/review work must remain untouched unless separately authorized. No backfill, provider test transaction or phone acceptance is authorized by this audit.

Private `expense_accounting_state` has revoked client/service table grants but no RLS enable in the prepared migration. Existing trigger-returning `enforce_expense_edit_authority` retains its historical ACL. These are defense-in-depth observations, not a demonstrated bypass; neither was expanded in this bounded repair. Existing request-expense-sync intentionally permits anon/authenticated JWT-bound callers and retains the live default service role execute grant; its internal actor authorization still applies.

## Regression handoff

Checkout: `/Users/jacksonsweet/Projects/OPS/.worktrees/ios-bugs-p5-accounting`, initially clean at `23cc8e0ff8f40f6434500e2b63393e2dc18cf7b6`, now clean at `816eec629967a09f5ca4ac041e3d44abc58c9e5f`.

Identity runner: `tests/runtime/expense-identity-lock-concurrency.py`. Set `OPS_EXPENSE_IDENTITY_FIXTURE=1`; supply `--psql BIN --socket /private/tmp/SOCKET --port NONDEFAULT --database DISPOSABLE_DB --user postgres --expect vulnerable` or `--expect repaired`. It requires a full captured P5/P7 synthetic fixture and verifies the exact production mutable JWT identity helper hashes; no constant identity stubs. It observes the exact holder PID blocking the waiter, then changes the binding and releases the lock. It extends only the helper's lock timeout for deterministic observation and restores the prior setting.

Expected red baseline: nine checks pass with `--expect vulnerable`, identifying returned old actor context/approval/accounting request/replayed correction after rebinding. Running `--expect repaired` against the original helper must fail on the first withdrawal case. Existing company-removal and new-correction defenses deny in both versions. Expected green: nine checks pass with the repaired helper, every newly rejected call returning 42501 and preserving expense/batch/notifications/ledger/queue/revision snapshots. An unchanged company wait still succeeds.

The exact original authority migration and helper are retained as `original-authority-migration.sql` and `original-identity-helper.sql`. Install only that original helper in the disposable fixture for red proof; restore the repaired helper from the changed authority migration for green. Do not revert the full P5 dispatcher to P4 behavior while testing P5/P7 callers.

Sequence runner: `tests/sql/expense-accounting-sequence-runtime.sql`, psql `-v expense_sequence_repaired=false` or `true`. Run updated `expense-accounting-fixture.sql` before the original lifecycle migration to reproduce the live default privileges. Red baseline must show all six actual role calls allowed; green must show all denied and unchanged sequence state, then successful owner allocation. `original-accounting-migration.sql` preserves the exact original lifecycle source. Running the repaired migration again also exercises sequence ACL replay repair. The existing complete accounting/correction/payroll suites remain the integration proof requirement, owned by root.

Verification performed here: Python AST parse and git diff checks only. No PostgreSQL test, cluster or provider operation ran in this subtask. Known prior suite counts are historical evidence and do not validate the two new changes.
